# Legacy seed helpers

These scripts are retained for historical or targeted maintenance work. The canonical product rebuild path is `../seed_rule_gold_db.py`, which consumes the workspace-level gold and rule-score artifacts.

